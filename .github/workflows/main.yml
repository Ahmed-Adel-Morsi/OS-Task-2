on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "Docker Hub Login"
        run: login --username ahmedmorsi --password dckr_pat_RCM7sOWtIoVCNXp6-64pvB2y6gQ